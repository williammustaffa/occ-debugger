export default {
  registered: false,
  valid: false,
  options: {
    // Enabler
    enabled: true,

    // Interface
    tagging: false,

    // Logging options
    cookies: false,
    topics: false,
    spinner: false,
    toJS: true,

    // Panels
    debuggerPanel: true,
  }
};