export default {
  registered: false,
  valid: false,
  options: {
    // Enabler
    enabled: true,

    // Logging options
    cookies: false,
    topics: false,
    spinner: false,
    toJS: true,

    // Panels
    debuggerPanel: true,
    layoutPanel: false,
    analyticsPanel: false
  }
};